{{- if .Values.persistentVolume.enabled | default false }}
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ include "scicat-elastic-search.fullname" . }}-pvc
  labels:
    {{- include "scicat-elastic-search.labels" . | nindent 4 }}
spec:
  storageClassName: {{ .Values.persistentVolume.storageClassName }}
  selector:
    matchLabels:
      app: {{ include "scicat-elastic-search.fullname" . }}
  accessModes:
    {{- toYaml .Values.persistentVolume.accessModes | nindent 4 }}
  resources:
    requests:
      storage: {{ .Values.persistentVolume.size }} 
{{- end }}