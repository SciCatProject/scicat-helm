{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "scicat-backend.serviceAccountName" . }}
  labels:
{{ include "scicat-backend.labels" . | indent 4 }}
{{- end -}}
